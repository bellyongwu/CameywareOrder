<Window x:Class="CameywareOrder.Views.LanguageSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:CameywareOrder.Localization"
    Title="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[Toolbar.Language], Mode=OneWay}"
    Height="460" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="/Assets/ICONS/app-icon.ico"
        Background="#F5F6FA">
    <Window.Resources>
        <Style x:Key="EnterButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="8">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#4F46E5" Offset="0"/>
                                    <GradientStop Color="#7C3AED" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Welcome banner -->
        <Border Grid.Row="0" Height="170" ClipToBounds="True">
            <Grid>
                <Image Source="/Assets/WELCOME%20PANEL/welcome_header_enter_system.jpg"
                       Stretch="UniformToFill"/>
                <Border>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#33000000" Offset="0"/>
                            <GradientStop Color="#CC000000" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="24,0,24,16">
                    <TextBlock Text="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[LanguageSelection.Welcome], Mode=OneWay}"
                               Foreground="White" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[LanguageSelection.WelcomeMessage], Mode=OneWay}"
                               Foreground="#F0F0F0" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Language options -->
        <StackPanel Grid.Row="1" Margin="32,24,32,0">
            <TextBlock Text="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[LanguageSelection.Prompt], Mode=OneWay}"
                       FontSize="14" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,14"/>
            <StackPanel x:Name="LanguageOptionsPanel"/>
        </StackPanel>

        <!-- Enter button -->
        <Button Grid.Row="2" Style="{StaticResource EnterButtonStyle}"
                Content="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[LanguageSelection.Enter], Mode=OneWay}"
                Height="46" Margin="32,18,32,26" Click="OnConfirmClick"/>
    </Grid>
</Window>
